import {Link} from 'react-router-dom';
import styled from 'styled-components';

export const LinkAdd = styled(Link)`background: #ff3311;`;
export const LinkEdit = styled(Link)`background: #00ffff; font-size: 12px;`;
export const LinkDelete = styled.a`background: #ff7700; font-size: 12px;`;
